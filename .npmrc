# Force npm to create portable lockfiles
engine-strict=true
save-exact=true
package-lock=true

# Disable platform-specific optimizations
prefer-offline=false
audit=false

# Use same npm version everywhere
# Note: use-node-version is deprecated in favor of .nvmrc or engines field in package.json